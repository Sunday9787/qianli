include ./qianli-ghost-button.pug
include ./qianli-badge-title.pug

mixin swiper(pagination = true)
  .swiper-container&attributes(attributes)
    .swiper-wrapper
      block wrapper

    if pagination
      .swiper-pagination
    else
      block pagination
