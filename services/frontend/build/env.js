const NODE_ENV_MAP = {
  development: 'development',
  production: 'production'
}

const APP_ENV_MAP = {
  dev: 'development',
  pro: 'production',
  test: 'test'
}

module.exports = {
  NODE_ENV_MAP,
  APP_ENV_MAP
}
